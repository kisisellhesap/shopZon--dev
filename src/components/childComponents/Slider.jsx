import SliderCard from "./SliderCard";
const Slider = () => {
  return (
    <div className="slider">
      <SliderCard />
    </div>
  );
};

export default Slider;

// slider için ayrı bir get isteği at
